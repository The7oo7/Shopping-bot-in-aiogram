# from aiogram import types,Dispatcher
# from aiogram import Bot
# import os
# bot = Bot(token=os.getenv('TOKEN'))
# dp = Dispatcher(bot)

# async def welcome(message: types.Message):
#      print(bot.send_message(message, 'hello'))

